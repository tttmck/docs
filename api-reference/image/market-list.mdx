---
title: "查询算力市场镜像"
api: "GET https://www.chenyu.team/api/open/v2/image/market/list"
description: "查询算力市场中可用的镜像列表"
---

# 查询算力市场镜像

获取算力市场中所有可用的镜像列表。

## 请求参数

此接口不需要任何请求参数。

## 响应参数

<ResponseField name="code" type="integer">
  响应码
</ResponseField>

<ResponseField name="msg" type="string">
  响应信息
</ResponseField>

<ResponseField name="data" type="object">
  返回数据
  
  <Expandable title="data">
    <ResponseField name="image_list" type="array">
      镜像列表
      
      <Expandable title="image_list">
        <ResponseField name="title" type="string">
          镜像名称
        </ResponseField>
        
        <ResponseField name="uuid" type="string">
          镜像uuid
        </ResponseField>
        
        <ResponseField name="size" type="number">
          镜像大小 GB
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## 代码示例

<CodeGroup>

```python Python
import requests

url = "https://www.chenyu.team/api/open/v2/image/market/list"
headers = {
    "Authorization": "Bearer your_api_key",
    "Content-Type": "application/json"
}

response = requests.get(url, headers=headers)
result = response.json()

print("算力市场镜像列表:")
for image in result['data']['image_list']:
    print(f"镜像名称: {image['title']}")
    print(f"镜像大小: {image['size']} GB")
    print(f"UUID: {image['uuid']}")
    print("---")
```

```javascript JavaScript
const axios = require('axios');

const url = 'https://www.chenyu.team/api/open/v2/image/market/list';
const headers = {
    'Authorization': 'Bearer your_api_key',
    'Content-Type': 'application/json'
};

axios.get(url, { headers })
    .then(response => {
        const data = response.data.data;
        console.log('算力市场镜像列表:');
        
        data.image_list.forEach(image => {
            console.log(`镜像名称: ${image.title}`);
            console.log(`镜像大小: ${image.size} GB`);
            console.log(`UUID: ${image.uuid}`);
            console.log('---');
        });
    })
    .catch(error => {
        console.error('Error:', error.response.data);
    });
```

```curl cURL
curl -X GET "https://www.chenyu.team/api/open/v2/image/market/list" \
  -H "Authorization: Bearer your_api_key" \
  -H "Content-Type: application/json"
```

</CodeGroup>

## 响应示例

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "image_list": [
      {
        "title": "Ubuntu 20.04 + CUDA 11.8",
        "uuid": "img_12345678-1234-1234-1234-123456789012",
        "size": 15.5
      },
      {
        "title": "PyTorch 2.0 + Jupyter",
        "uuid": "img_87654321-4321-4321-4321-210987654321",
        "size": 8.2
      }
    ]
  }
}
```